{
  "name": "User",
  "type": "object",
  "properties": {
    "user_type": {
      "type": "string",
      "enum": [
        "super_admin",
        "team_member",
        "student_coach",
        "student_team_member",
        "client"
      ],
      "default": "client",
      "description": "super_admin = Platform owner, team_member = Owner's team, student_coach = Independent health coach, student_team_member = Student's team, client = End user"
    },
    "parent_coach_id": {
      "type": "string",
      "description": "For student_team_member - ID of the student_coach they work for. For team_member - ID of super_admin"
    },
    "business_name": {
      "type": "string",
      "description": "Business name for student coaches and super admin"
    },
    "whitelabel_enabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether white-label branding is enabled"
    },
    "coach_id": {
      "type": "string",
      "description": "For clients - which coach/student owns them"
    }
  }
}